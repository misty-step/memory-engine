agents:
  queue: linux-small

steps:
  - label: ":dagger: ci"
    key: ci
    command: bash .buildkite/ci.sh
    artifact_paths:
      - "target/perf/action-latency-postgres-*"
    timeout_in_minutes: 60
