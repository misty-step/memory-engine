[package]
name = "memory-engine-web-shell"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "Rust web-shell dogfood host for memory-engine."

[dependencies]
memory-engine-core = { path = "../memory-engine-core" }
memory-engine-import = { path = "../memory-engine-import" }
memory-engine-service = { path = "../memory-engine-service" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"

[lints]
workspace = true
