[package]
name = "memory-engine-performance"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "Closed, privacy-safe v1 performance contract and mergeable histograms."

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"

[lints]
workspace = true
