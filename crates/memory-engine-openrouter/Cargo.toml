[package]
name = "memory-engine-openrouter"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "OpenRouter-dialect HTTP draft provider for memory-engine generation."

[dependencies]
memory-engine-generation = { path = "../memory-engine-generation" }
memory-engine-persistence = { path = "../memory-engine-persistence" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
ureq = { version = "3.2.0", features = ["json"] }

[lints]
workspace = true
