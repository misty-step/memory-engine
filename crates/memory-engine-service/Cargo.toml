[package]
name = "memory-engine-service"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "Typed Rust service boundary for memory-engine command workflows."

[dependencies]
memory-engine-core = { path = "../memory-engine-core" }
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.145"

[lints]
workspace = true
