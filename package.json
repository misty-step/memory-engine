{
  "name": "memory-engine",
  "version": "0.0.0",
  "description": "Shared learning engine kernel: canonical types, FSRS reference scheduler, deterministic grading.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./testkit": "./testkit/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "ci": "bun run typecheck && bun run check && bun test"
  },
  "dependencies": {
    "ts-fsrs": "^5.3.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.3.11",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
